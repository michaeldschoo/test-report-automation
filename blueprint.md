# Test Report Automation Blueprint

## 프로젝트 개요
본 프로젝트는 본사 사이트에 브랜치 관리자 계정으로 로그인하여 학생별, 회차별, 과목별 테스트 리포트를 자동으로 다운로드하고, 이를 학생별로 통합된 PDF 파일로 생성하는 웹 기반 업무 자동화 시스템입니다.

## 주요 기능
1. **자동 로그인**: 브랜치 관리자 계정을 사용하여 보안 접속 (로그인 필드 Role 기반 대응).
2. **메뉴 자동 탐색**: 테이블 구조 및 회차 번호를 기반으로 한 동적 링크 탐색.
3. **학생별 리포트 다운로드**: 
    - 각 과목별(Reading, Math, Thinking Skills, Writing) ZIP 다운로드.
    - 학생 ID 추출 및 폴더 분류(중복 방지 로직 포함).
4. **PDF 통합**: 학생별 폴더 내 과목별 PDF 통합 리포트 생성.

## 기술 스택
- **Language**: Python
- **Automation**: Playwright (브라우저 제어, ZIP 다운로드 모니터링)
- **PDF Processing**: PyPDF2 또는 pikepdf (병합 유틸리티)
- **Environment**: .env (비밀번호 보안)

## 프로젝트 구조
- `main.py`: 전체 자동화 프로세스 제어.
- `pdf_merger.py`: PDF 통합 로직.
- `downloads/`: 학생별 폴더 기반 임시 분류 폴더.
- `output/`: 최종 병합 PDF 저장 폴더.

## 업데이트 이력 및 주요 해결 사항
- **오류 처리 강화**: 
    - 로그인 실패 시 스크린샷(`debug_error.png`) 저장 및 수동 모드 제공.
    - PDF 병합 시 PermissionError(파일 열림) 및 경로 길이 제한 문제 해결.
- **분류 로직 고도화**:
    - 파일명 중복 제거를 위해 학생 폴더 내 파일명을 과목명으로 강제 고정.
    - 학생 이름 추출 시 여러 구분자(`_Selective`, `_Trial` 등)를 사용하여 데이터 누락 방지.
- **링크 탐색 유연성**:
    - 테이블 구조 기반 정밀 탐색 및 실패 시 정규식 기반 검색 로직 추가.
    - 각 과목별 페이지 진입 후 과목명 검증 추가.

## 유지보수 팁
- 오류 발생 시 `downloads/`와 `output/` 폴더를 정리 후 재실행하십시오.
- 특정 과목의 데이터가 누락되는 경우 사이트의 테이블 구조 변경 여부를 확인하십시오.
- PDF 병합 오류 발생 시, 실행 중인 PDF 뷰어에서 결과 파일을 닫아야 합니다.
